<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title><%= t('app.title') %></title>
    <link rel="stylesheet" href="../app-styles.css">
    <script src="../lib/feather.min.js"></script>
    <script src="../lib/echarts.min.js"></script>
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; font-src 'self' data:; img-src 'self' data:;">
</head>
<body>
    <div class="app-container">
        <%- include('components/sidebar/sidebar') %>
        <main class="content-area">
             <div id="view-container">
                <% modules.forEach(function(module, index) { %>
                    <div id="view-<%= module.id %>" class="view-section <%= index === 0 ? 'active' : '' %>">
                        <%- include(module.viewPath) %>
                    </div>
                <% }); %>
             </div>
        </main>
    </div>
    <script>
        window.translations = <%- JSON.stringify(translations) %>;
        feather.replace();
    </script>
    <script type="module" src="../renderer-entry.js"></script>
</body>
</html>
