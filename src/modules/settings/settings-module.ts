import { Module } from '../../core/module-manager';

export const SettingsModule: Module = {
    id: 'settings',
    nameKey: 'modules.settings',
    icon: 'settings',
    viewPath: 'views/settings'
};
