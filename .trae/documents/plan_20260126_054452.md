## 更新 SKILL.md 事件处理规范示例

### 需要修改的内容：

1. **第 86 行注释**：修正为"禁止对普通事件使用 emits，双向绑定事件必须使用 emits"

2. **第 102-154 行（正确示例）**：重构为符合规范的版本
   - `onUpdateModelValue` 使用 emits 模式（双向绑定事件）
   - `onChange`、`onClick`、`onFocus`、`onBlur` 使用 Props 回调模式（普通事件）
   - 添加 emits 选项定义

3. **第 247、254 行（父组件调用）**：更新为配合 emits 模式的正确调用方式

4. **第 8.1 节标题和说明**：明确区分双向绑定事件和普通事件的处理方式

### 修改原则：
- 双向绑定事件（`onUpdate:xxx`）→ emits 模式
- 普通事件（click、change、focus 等）→ Props 回调模式
- 严格遵守"一行代码 + 一行空格"的呼吸感排版
- 保持代码风格一致性