## 移除向后兼容代码计划

### 1. 修改 `types.ts`
- 删除 `SearchInputConfig` 接口（第24-42行）
- 删除 `SearchInputMergedConfig` 接口（第44-56行）

### 2. 修改 `input-search-props.ts`
- 删除 `SearchInputConfig` 类型导入（第13行）
- 删除 `config` prop 定义（第87-91行）
- 删除 "配置对象（向后兼容）" 注释区域（第85-91行）

### 3. 修改 `use-search-config.ts`
- 删除 `SearchInputMergedConfig` 类型导入（第12行）
- 重写 `mergedConfig` 逻辑，直接使用 props 而不是合并 config
- 简化配置合并逻辑，只处理默认值

### 4. 修改 `bn-input-search.tsx`
- 保持 `useSearchConfig` 的使用，但内部逻辑已简化
- 无需修改使用 `mergedConfig` 的代码

### 5. 修改 `monitor.tsx`
- 将 `config` prop 改为扁平化 props（`placeholder` 和 `size`）

### 6. 更新 `index.ts`
- 保持 `export type * from './variant-components/bn-input-search/types'`，因为 `SearchOption` 类型仍需导出