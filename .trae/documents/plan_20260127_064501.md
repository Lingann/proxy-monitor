# BnFormItem 组件目录结构重构计划

## 📋 重构目标
将 BnFormItem 相关的所有文件按照 Vue 组件规范组织到独立的 `sub-components/bn-form-item/` 目录下，实现组件的独立沙箱模式。

## 📁 目标目录结构
```
sub-components/bn-form-item/
├── composables/
│   ├── use-form-item-config.ts
│   ├── use-form-item-render.ts
│   └── use-form-item-state.ts
├── props/
│   └── form-item-props.ts
├── bn-form-item.tsx
└── index.ts
```

## 🔧 执行步骤

### 1. 创建新目录结构
- 创建 `sub-components/bn-form-item/composables/` 目录
- 创建 `sub-components/bn-form-item/props/` 目录

### 2. 移动文件
- 移动 `composables/item/use-form-item-config.ts` → `sub-components/bn-form-item/composables/`
- 移动 `composables/item/use-form-item-render.ts` → `sub-components/bn-form-item/composables/`
- 移动 `composables/item/use-form-item-state.ts` → `sub-components/bn-form-item/composables/`
- 移动 `props/form-item-props.ts` → `sub-components/bn-form-item/props/`
- 移动 `sub-components/bn-form-item.tsx` → `sub-components/bn-form-item/`

### 3. 更新导入路径
- 更新 `bn-form-item.tsx` 中的导入路径：
  - `../composables/item/xxx` → `./composables/xxx`
  - `../props/form-item-props` → `./props/form-item-props`
  - `../../types` → `../../types` (保持不变)
  - `../../shared/validation` → `../../shared/validation` (保持不变)

### 4. 创建统一导出文件
- 创建 `sub-components/bn-form-item/index.ts`，统一导出组件和 Props

### 5. 更新父级导出
- 更新 `props/index.ts`，移除 `form-item-props` 的导出
- 更新 `index.ts`，更新 BnFormItem 的导入路径

### 6. 清理旧目录
- 删除空的 `composables/item/` 目录

## ✅ 遵循的规范
- ✅ 独立沙箱模式：每个组件拥有独立的目录结构
- ✅ 目录名 = 文件名 = 组件名
- ✅ 中划线命名规范
- ✅ 统一导出：每个目录包含 `index.ts`
- ✅ 导出一致性：子模块不再单独导出