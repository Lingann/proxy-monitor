# 修复 TypeScript 类型错误

## 修复计划

### 1. 修复 `types.ts` 中的类型定义
- 将 `SearchInputConfig` 接口中的可选属性改为必需属性
- 或者创建一个新的 `SearchInputMergedConfig` 类型，确保所有必需属性都有默认值

### 2. 修复 `use-search-config.ts`
- 更新 `mergedConfig` 的返回类型为 `SearchInputMergedConfig`
- 确保所有属性都有明确的类型和默认值

### 3. 修复 `bn-input-search.tsx` 中的类型问题
- 修复 `modelValue` 的类型比较问题（移除与数字 `0` 的比较）
- 修复 `onClear` 事件调用，传递正确的事件参数
- 移除 `BnInput` 上不存在的 `onKeydown` 属性，改用原生 input 事件

### 4. 更新 `input-props.ts`（如需要）
- 检查是否需要调整 `onClear` 的类型定义

## 修改文件
1. `src/renderer/components/bn-input/variant-components/bn-input-search/types.ts`
2. `src/renderer/components/bn-input/variant-components/bn-input-search/composables/use-search-config.ts`
3. `src/renderer/components/bn-input/variant-components/bn-input-search/bn-input-search.tsx`
4. `src/renderer/components/bn-input/props/input-props.ts`（可能需要）