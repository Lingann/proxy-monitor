# bn-input 组件规范修复计划

## 🔴 严重问题修复（优先级：高）

### 1. 修复双向绑定事件处理（核心问题）
- **input.tsx**: 添加 `emits: ['update:modelValue']` 选项
- **input-props.ts**: 删除 `onUpdateModelValue` Props 定义
- **use-input-event.ts**: 修改为使用 emit 函数而非 props 回调
- **bn-search-input.tsx**: 修改为使用 emit 处理双向绑定
- **bn-password-input.tsx**: 修改为使用 emit 处理双向绑定

### 2. 修复 Ref 变量命名
- **input.tsx**: `type` → `typeRef`, `currentLength` → `currentLengthRef`, `isExceeded` → `isExceededRef`
- **bn-search-input.tsx**: `query` → `queryRef`
- **bn-password-input.tsx**: `visible` → `visibleRef`

## 🟡 次要问题修复（优先级：中）

### 3. 修复注释格式
- 将所有 `//` 注释替换为 `/* ... */` 单行块注释
- 涉及文件：input.tsx, use-input-event.ts, bn-search-input.tsx, bn-password-input.tsx

### 4. 优化代码风格
- **input.tsx**: 使用卫语句替换深层 if 嵌套
- 添加呼吸感空行（一行代码 + 一行空格）
- 优化函数调用，遵循极简调用规范

### 5. 修复国际化硬编码
- **input-props.ts**: 将 `placeholder: '请输入'` 改为使用 `useI18n`
- **bn-search-input-props.ts**: 将 `searchText: '搜索'` 改为使用 `useI18n`

### 6. 添加资源清理
- **input.tsx**: 在 `onUnmounted` 中清理资源（如有）
- **bn-password-input.tsx**: 在 `onUnmounted` 中清理 `visibleRef`

### 7. 统一事件处理模式
- **bn-search-input.tsx**: 移除 config 对象回调支持，统一使用 Props 回调
- 确保所有普通事件使用 Props 回调，双向绑定使用 emits

## 📝 修改文件清单

1. `src/renderer/components/bn-input/input.tsx`
2. `src/renderer/components/bn-input/props/input-props.ts`
3. `src/renderer/components/bn-input/composables/use-input-event.ts`
4. `src/renderer/components/bn-input/variant-components/bn-search-input/bn-search-input.tsx`
5. `src/renderer/components/bn-input/variant-components/bn-search-input/props/bn-search-input-props.ts`
6. `src/renderer/components/bn-input/variant-components/bn-password-input/bn-password-input.tsx`
7. `src/renderer/components/bn-input/variant-components/bn-password-input/props/bn-password-input-props.ts`

## ⚠️ 注意事项

- 双向绑定事件修复是破坏性变更，需要更新父组件的使用方式
- 确保所有修改后代码通过类型检查
- 保持组件功能不变，仅修复规范问题