移除 `color-mixins.scss` 文件及其文档引用：

1. 删除 `h:\lingann\proxy-monitor\src\renderer\styles\themes\mixins\color-mixins.scss` 文件
2. 更新 `NAMING_CONVENTIONS.md` 文档，移除第 70 行对 `color-mixins.scss` 的引用

该文件未被项目中任何其他文件使用，可以安全移除。