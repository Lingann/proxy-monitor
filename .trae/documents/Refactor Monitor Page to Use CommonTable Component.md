Refactor the `monitor` page to use the `CommonTable` component for all tables. The `settings` page does not contain any tables and requires no changes.

### 1. Update `monitor.ejs`
- Replace the existing `<table>` elements with container `<div>` elements for:
  - Process List Table (`#processTable` -> `#processTableContainer`)
  - Detail Server Table (`#detailServerTable` -> `#detailServerTableContainer`)
  - Detail Connection Table (`#detailConnectionTable` -> `#detailConnectionTableContainer`)
- Remove the manual table headers (`<thead>`) and body (`<tbody>`) as they will be generated by the component.

### 2. Update `monitor.ts`
- **Imports**: Import `CommonTable` and types (`Column`) from the component directory.
- **Interfaces**: Define TypeScript interfaces for `Process`, `ServerGroup`, and `Connection` data structures.
- **Initialization**:
  - Initialize three instances of `CommonTable` in `initMonitor`: `processTable`, `serverTable`, and `connectionTable`.
  - Define column configurations for each table, mapping the existing data fields and using `render` functions for formatting (e.g., `formatSpeed`, `getPurpose`, and action buttons).
- **Logic Updates**:
  - Replace direct DOM manipulation (setting `innerHTML`) with `tableInstance.setData(data)`.
  - Ensure the action buttons in the Process table continue to work by preserving the HTML structure in the `render` function (using the existing global `window.showDetails` handlers).

### 3. Verification
- Verify that all three tables (Process list, Server list in details, Connection list in details) render correctly.
- Verify that sorting and pagination (features of `CommonTable`) work.
- Verify that action buttons (Details, Limit Speed, Dropdown) still function correctly.
