更新 vue-code-skill.md 中的示例代码，使其符合 Ref 变量命名规范：

1. **第 9 节最佳实践示例**：
   - 将 `status` 改为 `statusRef`
   - 将 `updateTimestamp` 改为 `updateTimestampRef`
   - 更新所有使用这些变量的地方

2. **第 8.4 节父组件调用示例**：
   - 将 `inputValue` 改为 `inputValueRef`
   - 更新所有使用该变量的地方

所有修改将遵循"一行代码 + 一行空格"的呼吸感排版原则。