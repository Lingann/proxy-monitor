# Vue Code Skill 规则文件改进计划

## 主要改进内容

### 1. 移除错误示例
- 删除第85-105行的"❌ 错误示例"部分，只保留正确示例

### 2. 单行函数优化（9处）
- `handleClick` (L128-130, L246-248): 改为单行箭头函数
- `handleFocus` (L143-145, L217-219): 改为单行箭头函数  
- `handleBlur` (L147-149, L221-223): 改为单行箭头函数
- `handleInputChange` (L241-244): 优化为单行逻辑
- `handleRefresh` (L330-335): 优化为单行逻辑
- `handleStatusChange` (L337-344): 优化为单行逻辑

### 3. if语句优化（1处）
- L208-211: 使用卫语句替代大括号换行

### 4. Props注释规范化（3处）
- GoodComponent props (L113-120)
- createButtonProps 属性 (L180-187)
- UserStatusCard props (L314-319)

### 5. 代码风格统一
- 确保所有单行逻辑不使用大括号
- 保持"一行代码 + 一行空格"的呼吸感排版
- 优先使用卫语句和提前返回